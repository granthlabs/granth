{
  "name": "granth-monorepo",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/opfs-leader",
    "packages/*/*",
    "examples/playground"
  ],
  "scripts": {
    "build": "tsc -b",
    "test": "npm run build && npm run test -w opfs-leader && npm run test -w granth",
    "dev": "npm run dev -w playground",
    "clean": "rm -rf packages/*/*/dist packages/opfs-leader/dist"
  },
  "devDependencies": {
    "@types/react": "^19.2.18",
    "dexie": "^4.4.5",
    "fake-indexeddb": "^6.2.5",
    "react": "^19.2.8",
    "typescript": "^7.0.2",
    "vue": "^3.5.41"
  }
}
