{
  "name": "litie-workspace",
  "private": true,
  "type": "module",
  "workspaces": [
    "opfs-leader",
    "litie",
    "playground"
  ],
  "scripts": {
    "test": "npm run test -w opfs-leader && npm run test -w litie && npm run typecheck",
    "test:browser": "npm run test -w playground",
    "dev": "npm run dev -w playground",
    "typecheck": "tsc -p tsconfig.json"
  },
  "devDependencies": {
    "dexie": "^4.4.5",
    "fake-indexeddb": "^6.2.5",
    "typescript": "^7.0.2"
  }
}
